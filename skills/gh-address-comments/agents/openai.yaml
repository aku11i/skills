interface:
  display_name: "Address PR Comments"
  short_description: "Fix and resolve actionable PR review comments"
  default_prompt: "Use $gh-address-comments to inspect actionable pull request review comments, apply needed fixes, commit and push them, then reply to and resolve the threads."
