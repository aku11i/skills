interface:
  display_name: "Create Draft PR"
  short_description: "Create or update a GitHub draft PR"
  default_prompt: "Use $gh-create-draft-pr to commit, push, and open a draft pull request for the current changes."
